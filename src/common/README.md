# Common (공통 모듈)

프로젝트 전역에서 사용되는 공통 유틸리티 모듈입니다.
로깅, 에러 처리, 미들웨어, 헬퍼 함수 등 모든 모듈이 의존하는 기반 코드를 포함합니다.

## 구조
- `logger/` — Winston 기반 로깅 설정 및 포맷터
- `errors/` — 커스텀 에러 클래스 및 에러 핸들링
- `middleware/` — Express 미들웨어 (인증, 검증, 에러 캐치)
- `utils/` — 범용 헬퍼 함수 (날짜, 문자열, 파일 처리 등)
