# Web (관리 웹 UI)

프로젝트 관리를 위한 웹 인터페이스 모듈입니다.
Express + EJS + Tailwind CSS 기반으로 캐릭터, 에피소드, 영상 파이프라인을 제어합니다.

## 구조
- `routes/` — Express 라우트 정의
- `views/` — EJS 템플릿 (페이지, 컴포넌트, 레이아웃)
- `public/` — 정적 파일 (CSS, JS, 이미지)
